version: '3'

services:
  nginx:
    container_name: nginx
    # image: tiangolo/nginx-rtmp:${NGINX_VERSION}
    image: nginx:${NGINX_VERSION}
    restart: always
    ports:
      - "${NGINX_HTTP_HOST_PORT}:80"
      - "${NGINX_HTTPS_HOST_PORT}:443"
      # - "${NGINX_PUSH_STREAM_PORT}:1935"
    volumes:
      - ${CONF_DIR}/nginx/conf.d:/etc/nginx/conf.d/:rw
      # - ${CONF_DIR}/nginx/rtmp.d:/etc/nginx/rtmp.d/:rw
      - ${CONF_DIR}/nginx/php-fpm.d:/etc/nginx/php-fpm.d/:rw
      - ${CONF_DIR}/nginx/nginx.conf:/etc/nginx/nginx.conf:rw
      - ${CONF_DIR}/nginx/ssl:/ssl:rw
      # - ${DATA_DIR}/nginx/rtmp:/tmp/data/rtmp/:rw
      - ${LOGS_DIR}/nginx:/var/log/nginx:rw
      - ${SOURCE_DIR}:/code/:rw

  php-fpm-7.4:
    container_name: php-7.4
    build: ./conf/php-7.4
    # image: php:${PHP_VERSION}-fpm
    restart: always
    expose:
      - 9000
    volumes:
      - ${CONF_DIR}/php-7.4/php.ini:/usr/local/etc/php/php.ini:rw
      - ${CONF_DIR}/php-7.4/php-fpm.conf:/usr/local/etc/php/php-fpm.conf:rw
      - ${CONF_DIR}/php-7.4/php-fpm.d:/usr/local/etc/php/php-fpm.d
      - ${SOURCE_DIR}:/code/

  php-fpm-8.2:
    container_name: php-8.2
    build: ./conf/php-8.2
    # image: php:${PHP_VERSION}-fpm
    restart: always
    expose:
      - 9000
    volumes:
      - ${CONF_DIR}/php-8.2/php.ini:/usr/local/etc/php/php.ini:rw
      - ${CONF_DIR}/php-8.2/php-fpm.conf:/usr/local/etc/php/php-fpm.conf:rw
      - ${CONF_DIR}/php-8.2/php-fpm.d:/usr/local/etc/php/php-fpm.d:rw
      - ${SOURCE_DIR}:/code/:rw

  mariadb:
    container_name: mariadb
    image: mariadb:${MARIADB_VERSION}
    restart: always
    expose:
      - 3306
    ports:
      - "${MARIADB_PORT}:3306"
    volumes:
      - ${DATA_DIR}/mariadb:/var/lib/mysql/:rw
      - ${LOGS_DIR}/mariadb:/var/log/mysql/:rw
    environment:
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
      MYSQL_ROOT_HOST: "${MYSQL_ROOT_HOST}"

  redis:
    container_name: redis
    image: redis:${REDIS_VERSION}
    restart: always
    expose:
      - 6379
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - ${CONF_DIR}/redis/redis.conf:/usr/local/etc/redis/redis.conf:rw
      - ${DATA_DIR}/redis:/data:rw
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]

  rabbitmq:
    container_name: rabbitmq
    image: rabbitmq:${RABBITMQ_VERSION}
    restart: always
    expose:
      - 5672
    ports:
      - "${RABBITMQ_PORT}:5672"
      - "${RABBITMQ_WEB_PORT}:15672"
    volumes:
      - ${CONF_DIR}/rabbitmq/conf.d:/etc/rabbitmq/conf.d:rw
      - ${LOGS_DIR}/rabbitmq:/var/log/rabbitmq:rw
      - ${DATA_DIR}/rabbitmq:/var/lib/rabbitmq:rw
      - ${DATA_DIR}/rabbitmq/mnesia:/var/lib/rabbitmq/mnesia:rw

  mongodb:
    container_name: mongodb
    image: mongo:${MONGO_VERSION}
    restart: always
    expose:
      - 27017
    ports:
      - ${MONGO_PORT}:27017
    volumes:
      - ${DATA_DIR}/mongo/db:/data/db:rw
      - ${DATA_DIR}/mongo/key:/mongo:rw
      - ${DATA_DIR}/mongo/configdb:/data/configdb:rw
    environment:
      MONGO_INITDB_ROOT_USERNAME: "${MONGO_INNTDB_ROOT_USERNAME}"
      MONGO_INITDB_ROOT_PASSWORD: "${MONGO_INITDB_ROOT_PASSWORD}"

